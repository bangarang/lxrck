<% provide(:body_class, "top") %>
<% provide(:title, @item.name) %>
<div class="goldtainer">

	<h1>Editing item</h1>

	<%= render 'form' %>

	<%= link_to 'Show', @item %> |
	<%= link_to 'Back', items_path %>
	
</div>