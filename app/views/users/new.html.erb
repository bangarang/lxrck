<% provide(:body_class, "sign_in") %>
<div id='clearance' class='sign-up goldtainer'>
  <h2>Sign up</h2>
  <div class='sign-in'>
    <div class="innered">
    <%= form_for @user do |form| %>
      <%= render :partial => '/users/form', :object => form %>

      <div class='submit-field'>
        <%= form.submit %>
      </div>

      <div class='other-links'>
        <%= link_to t('.sign_in'), sign_in_path %>
      </div>
    <% end %>

      </div>
  </div>
</div>
